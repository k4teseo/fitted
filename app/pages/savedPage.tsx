import { View, Text, TextInput} from 'react-native';

export default function SavedPage() {
    return (
        <View>
            <Text>Saved</Text>
            <TextInput placeholder="Save" />
        </View>
    );
}